<%#<div class="side_bar>%>
<div class="side-bar-header">
  <div class="header-name">
    <%= link_to edit_user_path(current_user) do %>
      <%= current_user.name %>
    <% end %> <%#「rails routes」コマンドを実行し、どのようなリンク先を指定すれば、該当のアクションが動くのか確認しましょう。%>
              <%#ユーザーの編集機能へのリンクは、以下が該当するのがわかります。%>
  <%#edit_user	GET	/users/:id/edit(.:format)	users#edit %>
  <%#link_toのパスを、ログインしているユーザーの編集をするため 、
edit_user_path(current_user)と指定します。
current_user.nameは、ログインしているユーザー名です。
ユーザー名をクリックすると、usersコントローラーのeditアクションが実行され、編集ページに遷移できます。%>
  
  </div>
  <div class="create-room">
    <%= link_to "チャットを作成する", new_room_path %>
  </div>
</div>

<div class= "rooms">
 <% current_user.rooms.each do |room|%>
  <div class="room">
    <div class="room-name">
      <%= link_to room.name, room_messages_path(room)%> <%#「name」は、roomsテーブルのカラム名%>
    </div>
  </div>
 <% end %>
</div>
<%#/div%%>